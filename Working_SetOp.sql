SELECT Empno, Ename,Job,Sal
 FROM EMP WHERE SAL BETWEEN 1000 and 3000
UNION
SELECT Empno, Ename, Job, Sal
from EMP WHERE SAL BETWEEN  2000 and 5000;


SELECT Empno, Ename,Job,Sal
 FROM EMP WHERE SAL BETWEEN 1000 and 3000
UNION ALL
SELECT Empno, Ename, Job, Sal
from EMP WHERE SAL BETWEEN  2000 and 5000